services:
  planner:
    build: .
    image: planner:latest
    container_name: planner
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      # Proces draait in UTC; de planner rekent zelf in PLANNER_TZ.
      TZ: UTC
      PLANNER_TZ: Europe/Amsterdam
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
